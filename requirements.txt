# --- dependencias de ejecución (el workflow instala esto) ---
httpx[http2]>=0.27,<1.0   # HTTP/2, timeouts y pool de conexiones
rapidfuzz>=3.0            # similitud de títulos (mucho más rápida que difflib)
